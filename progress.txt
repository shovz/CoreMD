# Progress Log

## Learnings
- US-003: When adding a notes sidebar alongside existing content, restructure `main` from `flex-col` to `flex` (row) so the notes panel can sit as a flex sibling to the content area — avoids wrestling with absolute/fixed positioning for a persistent sidebar.

---
